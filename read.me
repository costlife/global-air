npm install && npm start

npm install过慢时可以用（淘宝镜像）
npm install --registry http://registry.npm.taobao.org/
使用react + redux 搭建的业务开发框架


#online
1. sh build.sh
2. 会在根目录下生成一个"时间戳_global_air_release.tar.bz2"的压缩包
3. 这个包解压缩后会生成opt/intl/frontPage的目录，该目录下，static为静态资源，templates是模版
4. 将该包进行发布解压缩上线
5. 需要知道上线以后的domain，添加到webpack-production.config.js中，会自动在模版中引用静态资源的地方加上domain